// THE MONTH-CLOSE DOCKET — the one stream everything that wants the player
// arrives through: attention items, this month's tape, deliveries, balloons,
// expirations. Replaces the old InboxRail mount; until the full docket lands
// this passes through to it so nothing regresses in the meantime.
import InboxRail from "@/ui/InboxRail";

export default function Docket() {
  return <InboxRail />;
}
