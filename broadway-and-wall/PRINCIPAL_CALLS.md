# The Principal — owner calls (final)

Locked for implementation. Realism over preference; difficulty is an output.

1. **Succession** — continue as the heir. Portfolio and bench survive; relationships (`sponsor.events`, `lenderRel`, `StreetTie`, `hireReputation`) reset with the dead principal.
2. **Attributes** — player sees their own; never anyone else's. Narrow others by dealing (`obs` / `band`).
3. **Fund path** — optional; balance-sheet / family-office path remains the default.
4. **Fund cash** — second cash account (fund vs GP). Promote is a transfer between them. Top bar shows GP liquidity; fund cash is vehicle cash. One function per quantity.
5. **Estate** — rate is a world fact (~40% above exemption, cited). Full counterplay: step-up, §6166, insurance, gifting, ground leases. Do not soften the rate.
6. **Hire bands** — never fully close.
7. **Firm count** — bound by product (deal flow), not a headcount rail. Genealogy proposes; the raise can still refuse.
8. **`pnpm staff`** — freeze through Phase 1 unedited. At Phase 3 (careers), rewrite the oracle and say in the commit whether assertions are "regressed" or "no longer the design."

Also locked:

- **`s.peopleRng`**, death month drawn once at creation (SSA 2019 male period table, cited in `people.ts`).
- **Net worth** — top bar = firm going-concern equity; estate net-of-tax is a separate readout when succession is in view.
- **Succession must not set `gameOver`** (frozen-world trap).

See `HANDOFF_PRINCIPAL.md` for the full design.
