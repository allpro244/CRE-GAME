// THE RECORD — the firm's history on one scrubbable strip: every deed,
// delivery, exit, refinancing and near-death since founding, with the year
// in review at the top. Placeholder until the timeline lands.
export default function FirmTimeline() {
  return (
    <div className="hint" style={{ padding: 16 }}>
      The record is being written.
    </div>
  );
}
